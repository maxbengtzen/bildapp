{
  "name": "gridprint-web",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@fontsource/bricolage-grotesque": "^5.2.8",
    "pdfjs-dist": "^5.4.149",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^5.1.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.12",
    "concurrently": "^9.2.1",
    "daisyui": "^5.1.6",
    "tailwindcss": "^4.1.12"
  },
  "scripts": {
    "start": "concurrently \"npm run watch:css\" \"react-scripts start\" --names \"CSS,REACT\" --prefix-colors \"cyan,blue\"",
    "build": "npm run build:css && react-scripts build",
    "build:css": "tailwindcss -i src/styles.css -o src/index.css --build",
    "watch:css": "tailwindcss -i src/styles.css -o src/index.css --watch",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint src --ext .js,.jsx",
    "lint:fix": "eslint src --ext .js,.jsx --fix"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:5000"
}
